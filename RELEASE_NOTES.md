# RE word 词汇助手 v1.3.0 发布说明

> SiYuan（思源笔记）插件：离线词典查词 + 词库复习 + AI 精读 + EPUB 阅读器，面向英语阅读与词汇积累。

## 本次更新重点：让 AI 翻译「记得」整本书

### 1. 本书前提上下文（新增）

AI 批量翻译原本是**无状态**的——每次只看到当前这批发过去的段落，不知道前文已经把 `Sludge` 译成「斯拉奇（狗名）」，于是后文又翻成「烂泥」。

现在你可以为每本书手写一份**前提上下文**（人物对照 / 背景设定 / 译法要求），翻译时自动拼进 AI 提示词，全书专有名词保持一致：

- **思源原生编辑体验**：编辑区直接复用思源的块编辑器（lite Protyle），支持列表、加粗、表格等完整排版，写起来和记笔记一样。
- **只作用于当前这本书**，互不干扰。
- **实时成本提示**：编辑时下方显示 `字数 ≈ token 数`，超过 1500 字转橙色提醒——上下文会随每次翻译请求发出，写太长会持续消耗 token。建议 200~800 字。
- 上下文以 Markdown 原文发送给 AI（比 HTML 省 60% 以上 token），Lute 只用于编辑预览。

> 位置：阅读器 → ⚙ 设置 → 🌐 双语对照 → **本书上下文**

### 2. Token 用量监测

- 顶栏「双语」按钮悬停可看本次翻译的输入 / 输出 token。
- 设置面板新增**本书 Token** 累计统计（跨批次累加、持久化保存），并带一键重置。

### 3. 生词本导出

把当前已显示的双语对照导出为 **Markdown 表格**（原文 | 译文），一键复制到剪贴板，直接粘贴进思源笔记即可做成复习文档。

> 位置：阅读器 → ⚙ 设置 → 🌐 双语对照 → **生词本导出**

### 4. 译文排版优化

针对中英混排观感做了全面调整：

- 译文字号缩小至 **0.62em**（CJK 字体实际渲染尺寸显著大于英文衬线字体，缩小后与正文视觉相当）。
- **与原文左右对齐**：继承段落首行缩进，修正译文左边缘不齐的问题。
- 行高收至 1.45、段间距收紧、透明度调至 0.82、CJK 字间距微调，整体更贴近书籍自身排版。

### 5. 其它

- 修复一处遗留 bug：顶栏双语按钮的 token 提示因引用了未定义变量而从未生效。
- 收敛 AI 翻译链路的调试日志，发布版控制台不再刷屏（保留异常告警）。

---

## 功能概览（历史积累）

- **离线词典查词**：基于自建 MDX / StarDict 离线词典（如 NCECD、ECD2），无需联网，支持词形还原与相似词候选。
- **悬浮取词（Option + 悬停）**：在阅读器正文中按住 `Option` 把鼠标移到英文单词上即弹出富词卡（音标 / 词性 / 释义 / 例句）。
- **划词工具栏**：选中文本后可一键「词典 / 翻译 / 朗读 / 批注 / 复制 / 发送 AI」。
- **词卡「加入词库」★**：悬浮词卡与划词词卡均可一键收藏 / 移出词库。
- **AI 精读面板**：学习 / 对话双模式，支持把思源文档页签拖入作为上下文。
- **EPUB 阅读器**：标注 / 批注（零正文污染、卸载无残留）、目录、书签、双语对照、字体与排版精细控制。
- **分类字体系统**（v1.2.0）：衬线 / 无衬线 / 等宽三条独立字体链，自动改写 EPUB 内部字体声明。
- **阅读 Tab ⌘E 最近文档**：在阅读 Tab 内按 `⌘E` 唤出思源原生「最近打开文档」切换器。

## 安装

1. 在 SiYuan 集市搜索 `RE word` 或 `siyuan-plugin-reword` 安装 / 更新；
2. 或下载本 Release 的 `package.zip`，在「设置 → 集市 → 已下载 → 导入」中手动安装。

## 配置建议

- 词典：在插件设置中导入你的 MDX / StarDict 原包。
- AI 精读：在「AI 设置」中填入 API Key 与模型；可设置「翻译提示词」。
- 双语阅读：首次使用建议先花一两分钟填「本书上下文」，翻译一致性会有明显提升。

## 技术栈

TypeScript + Svelte + SiYuan API + foliate-js（EPUB 渲染）。

## 许可证

GPL-3.0
