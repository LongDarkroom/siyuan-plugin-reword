# RE word 词汇助手 v1.3.3 发布说明

> SiYuan（思源笔记）插件：离线词典查词 + 词库复习 + AI 精读 + EPUB 阅读器，面向英语阅读与词汇积累。

## 本次更新重点：翻译缓存「书名明确」+「按页统计」，以及稳定性加固

> 在 v1.3.2 双语「页数可调 + 缓存持久化」基础上，进一步让缓存**看得见、认得出、查得清**；
> 并对全库做了一轮稳定性体检与加固，让"静默失败"变成"可观测、可排查"。

### 1. 翻译缓存明确所属书籍（新增）

- 设置面板 **翻译缓存** 区块顶部现在显示当前查看书籍的书名：📖 《书名》。
- 新增 **「选择书籍」下拉**：当存在多本有缓存的书籍时，可一键切换到任意一本，查看其缓存统计（无需打开该书）。
- 书名索引随首次翻译自动建立（`translations/_index.json`），重开书后能准确回显。

### 2. 翻译缓存按页统计（新增）

- 缓存区块实时显示：**共缓存 N 页，第 X-Y 页缓存成功**（如「共缓存 4 页，第 1-4 页缓存成功」）。
- 连续页自动合并为区间（如第 1、2、3、5 页 → 展示「第 1-3 页、第 5 页」），离散页分列。
- 统计口径为「已成功翻译并落盘的节（内容文档序号，1-based）」，与「已缓存 N 段」并列展示。

### 3. 与缓存机制的整体语义

- 已翻译的释义保存在本地（按书），**重开书籍与翻页都不会消失**；同一段文字命中缓存零 token 重译。
- 切换查看的书籍只改变面板展示，不影响任何缓存数据；**清空**按钮仅删除当前所选书籍的缓存，换书互不影响。

> 位置：阅读器 → ⚙ 设置 → 🌐 双语对照 → **翻译缓存**

### 4. 衔接说明

- 翻译预取页数（v1.3.2）仍为可调设置（0–8 面，默认 2 面），变更后已开启双语会立即增量刷新入缓存。
- 本书前提上下文、本书 Token、生词本导出等能力保持不变。

### 5. 稳定性加固（本次重点工程项）

体检发现两类长期隐患，本次一并处理：

**（1）消灭「静默失败」——约 188 处空捕获全部登记**

- 全库原本有约 188 处「catch 后仅跟一句注释」的空捕获：异常被完全吞掉，不弹提示、不写日志。
  线上出问题时没有任何信号，用户只看到"点了没反应"，排查全靠猜。
- 本次统一改造为 `core/safe.ts` 新提供的 `logSwallow()` 登记调用：
  **严格保持原有「吞掉异常、继续执行」的语义**（只改 catch 体内，不动任何控制流，零行为变更风险），
  仅补记一条运行日志，让失败变得可观测。
- **分级记录**，不刷屏、不遗漏：
  - `error`（12 处）：持久化落盘失败——翻译缓存 `saveMeta`、copilot 三个 `save*`、`persistBookTokenUsage`、
    `persistSort`、本书上下文 `savePrimer` 等，属**用户数据丢失风险**。
  - `warn`（7 处）：用户可感知的功能失效——剪贴板复制失败等。
  - `debug`（169 处）：预期内容错——JSON 解析试探、localStorage 不可用降级等。
- 标签带上下文（如 `index.ts · initDictionary`、`try { await this.saveData("copilot-ai.json", ...) }`），
  可在插件的运行日志面板中直接检索定位。

**（2）清理死代码，消除误改风险**

- 移除 `src/copilot-src/`（约 4 万行，含 19,054 行的 `ai-sidebar.svelte`）。该目录为历史 copilot 分支，
  已无任何代码 import、且不参与打包，却与在用的 `src/copilot/` 并行存在，极易误导维护。
- 迁移其仍被依赖的**思源全局环境类型**（`BlockId`/`DocumentId`/`IResReadDir`/`Window.siyuan` 等）
  至 `src/siyuan/global-types.d.ts`（保留 GPL-3.0 原始署名），避免删除后类型丢失。

**（3）新增可复跑的治理脚本**

- `scripts/wire-silent-catch.mjs`：幂等的 codemod，可反复扫描并登记新产生的空捕获，
  自动排除 `src/core/`（可观测层不能给自己装可观测，否则递归）。

> 验证：构建全绿（vite + `tsc --noEmit`）；全量 87 个测试文件的通过/失败清单与改造前**逐项一致**，无回归。

---

## 功能概览（历史积累）

- **离线词典查词**：基于自建 MDX / StarDict 离线词典（如 NCECD、ECD2），无需联网，支持词形还原与相似词候选。
- **悬浮取词（Option + 悬停）**：在阅读器正文中按住 `Option` 把鼠标移到英文单词上即弹出富词卡（音标 / 词性 / 释义 / 例句）。
- **划词工具栏**：选中文本后可一键「词典 / 翻译 / 朗读 / 批注 / 复制 / 发送 AI」。
- **词卡「加入词库」★**：悬浮词卡与划词词卡均可一键收藏 / 移出词库。
- **AI 精读面板**：学习 / 对话双模式，支持把思源文档页签拖入作为上下文。
- **EPUB 阅读器**：标注 / 批注（零正文污染、卸载无残留）、目录、书签、双语对照、字体与排版精细控制。
- **分类字体系统**（v1.2.0）：衬线 / 无衬线 / 等宽三条独立字体链，自动改写 EPUB 内部字体声明。
- **本书前提上下文**（v1.3.0）：为每本书手写背景 / 人物 / 译法，注入 AI 翻译提示词，全书专有名词保持一致。
- **翻译缓存**（v1.3.2 / v1.3.3）：按书落盘、可调预取页数、可查看与清空；v1.3.3 起明确书名、支持「选择书籍」下拉、按页统计「共缓存 N 页，第 X-Y 页缓存成功」，重开书 / 翻页释义不消失。
- **稳定性可观测**（v1.3.3）：约 188 处静默空捕获改造为分级登记（error 落盘失败 / warn 功能失效 / debug 预期容错），异常不再无声消失；配套幂等治理脚本，可反复扫描新增代码。

## 安装

1. 在 SiYuan 集市搜索 `RE word` 或 `siyuan-plugin-reword` 安装 / 更新；
2. 或下载本 Release 的 `package.zip`，在「设置 → 集市 → 已下载 → 导入」中手动安装。

## 配置建议

- 词典：在插件设置中导入你的 MDX / StarDict 原包。
- AI 精读：在「AI 设置」中填入 API Key 与模型；可设置「翻译提示词」。
- 双语阅读：首次使用建议先花一两分钟填「本书上下文」，翻译一致性会有明显提升；预取页数按设备性能与阅读节奏设 1–3 面即可。

## 技术栈

TypeScript + Svelte + SiYuan API + foliate-js（EPUB 渲染）。

## 许可证

GPL-3.0
