# RE word 词汇助手 v1.2.0 发布说明

> SiYuan（思源笔记）插件：离线词典查词 + 词库复习 + AI 精读 + EPUB 阅读器，面向英语阅读与词汇积累。

## 本次更新重点

### 1. 分类字体系统（新增，参考 Readest）

阅读器字体设置新增**「分类」模式**，把字体拆成三条独立的链，与原有的「跟随思源 / 自定义 / 系统默认」并列可选：

| 链 | 用途 | 默认字体 |
|----|------|----------|
| **衬线（Serif）** | 正文（长文阅读更省力） | 霞鹜文楷 |
| **无衬线（Sans-Serif）** | 正文（小字号更清晰） | 苹方 |
| **等宽（Monospace）** | 代码块 `<pre>` / `<code>` / `<kbd>` 专用 | Fira Code |

- 可单独指定**中文字体**，它会插入到每条链的次位；留空则用跨平台 CJK 兜底栈。
- 每条链都自带完整的 CJK 兜底——西文衬线 / 等宽字体没有汉字字形，缺了兜底中文会掉到系统默认字体，跨平台观感参差。
- **EPUB 关键词重写**：书籍自身的 `h1 { font-family: serif }`、各类 class 段落原本会绕过阅读器设置，现在会自动改写为对应的字体链，让整本书真正统一。改写只替换兜底族、保留书籍自带字体名（`Georgia, serif` → `Georgia, 你的衬线链`），属于最小侵入。
- 未安装的字体会被浏览器自动跳过，由链尾兜底接管，不会出现方块或乱码。

> 默认仍是「跟随思源」（零配置行为不变），需要精细控制再切到「分类」。

### 2. 双语对照修复与美化

- **修复同一段出现多条译文**：此前注入的译文节点会被段落选择器再次收集中，当成新段落重复翻译并嵌套自身。现在收集阶段会先排除已注入的译文节点。
- **译文改用思源字体**：此前译文继承书籍的英文衬线字体，中文渲染发虚。现在显式注入思源阅读字体栈与正文色（EPUB 内容在 iframe 内，读不到思源的 CSS 变量，因此必须传具体值）。
- **译文字号独立可调**（默认 0.70em，可在设置中 0.6–1.0 微调），比正文小一号、紧贴原文下方，观感对齐 Readest。
- 译文保持极简：无边框、无底色、无圆角，作为内容的一部分自然融入。

### 3. 阅读面板美化

- 顶栏、底栏改为悬浮药丸条，进度条改为细滑块 + 主色圆点。
- **搜索按钮从底栏移到顶栏**——原先在底栏最右会被思源悬浮窗（三个点）遮挡。
- 修复进度条在某些主题下不可见的问题。
- 新增**段落悬停高亮**：鼠标悬停段落时轻微底色，提升阅读定位感（可在设置中关闭）。

### 4. 其它

- 收敛双语模块的调试日志，发布版控制台不再刷屏（保留异常告警）。
- 修复一处遗留隐患：`reader-settings.ts` 的 TS 参数属性写法会导致部分测试环境无法解析，已改为兼容写法。

## 功能概览（历史积累）

- **离线词典查词**：基于自建 MDX / StarDict 离线词典（如 NCECD、ECD2），无需联网，支持词形还原与相似词候选。
- **悬浮取词（Option + 悬停）**：在阅读器正文中按住 `Option` 把鼠标移到英文单词上即弹出富词卡（音标 / 词性 / 释义 / 例句）。
- **划词工具栏**：选中文本后可一键「词典 / 翻译 / 朗读 / 批注 / 复制 / 发送 AI」。
- **词卡「加入词库」★**：悬浮词卡与划词词卡均可一键收藏 / 移出词库。
- **AI 精读面板**：学习 / 对话双模式，支持把思源文档页签拖入作为上下文。
- **EPUB 阅读器**：标注 / 批注（零正文污染、卸载无残留）、目录、书签、双语对照、字体与排版精细控制。
- **阅读 Tab ⌘E 最近文档**：在阅读 Tab 内按 `⌘E` 唤出思源原生「最近打开文档」切换器。

## 安装

1. 在 SiYuan 集市搜索 `RE word` 或 `siyuan-plugin-reword` 安装 / 更新；
2. 或下载本 Release 的 `package.zip`，在「设置 → 集市 → 已下载 → 导入」中手动安装。

## 配置建议

- 词典：在插件设置中导入你的 MDX / StarDict 原包。
- AI 精读：在「AI 设置」中填入 API Key 与模型；可设置「翻译提示词」。
- 阅读器字体：默认「跟随思源」即可；想精细控制（尤其技术书的代码块）可切到「分类」模式。

## 技术栈

TypeScript + Svelte + SiYuan API + foliate-js（EPUB 渲染）。

## 许可证

GPL-3.0
