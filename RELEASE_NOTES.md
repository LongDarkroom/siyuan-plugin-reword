# RE word 词汇助手 v1.0.0 发布说明

> SiYuan（思源笔记）插件：离线词典查词 + 词库复习 + AI 精读，面向英语阅读与词汇积累。

## 功能亮点

- **离线词典查词**：基于自建 MDX / StarDict 离线词典（如 NCECD、ECD2），无需联网，支持词形还原与相似词候选。
- **悬浮取词（Option + 悬停）**：在阅读器正文中按住 `Option` 把鼠标移到英文单词上即弹出富词卡（音标 / 词性 / 释义 / 例句），松开或移开自动收起；与「划词 → 工具栏 → 词典」并存。
- **划词工具栏**：选中文本后可一键「词典 / 翻译 / 朗读 / 批注 / 复制 / 发送 AI」，翻译改为直接发送到 AI 精读面板（不再弹内联译文，交互更聚焦）。
- **词卡「加入词库」★**：悬浮词卡与划词词卡均可一键收藏 / 移出词库。
- **AI 精读面板**：学习 / 对话双模式，支持把思源文档页签拖入作为上下文；「翻译」按钮可配置专属提示词（在 AI 设置中），选中文本后自动打开面板并发送。
- **阅读 Tab ⌘E 最近文档**：阅读 Tab 与思源文档 Tab 体验对齐——在正文内按 `⌘E` 唤出思源原生「最近打开文档」切换器（通过按键转发实现，不破坏原生交互，阅读 Tab 不被顶掉）。
- **词典卡折叠交互**：词组·短语、全部义项、词性分组、例句均可展开 / 收起。
- **标注与高亮**：阅读器内标注、批注，零正文污染、卸载无残留。

## 安装

1. 在 SiYuan 集市搜索 `RE word` 或 `siyuan-plugin-reword` 安装；
2. 或下载本 Release 的 `package.zip`，在「设置 → 集市 → 已下载 → 导入」中手动安装。

## 配置建议

- 词典：在插件设置中导入你的 MDX / StarDict 原包。
- AI 精读：在「AI 设置」中填入 API Key 与模型；可设置「翻译提示词」。

## 技术栈

TypeScript + Svelte + SiYuan API + foliate-js（EPUB 渲染）。

## 许可证

GPL-3.0
